# Raw Data

Authenticate to Kaggle and run:

```bash
python src/download_data.py --source auto
```

The expected official file is commonly named `Combined_Data.csv`. Raw CSV files are intentionally excluded from Git.
